# Pascal AI Assistant - FIXED Requirements for Raspberry Pi 5
# Compatible and tested versions that resolve aiohttp issues

# CRITICAL: HTTP client - Use stable version that works on ARM64
aiohttp==3.9.5
aiofiles==23.2.0

# Alternative if 3.9.5 has issues: 
# aiohttp==3.8.6

# Core networking and async
requests==2.31.0

# Configuration and utilities
python-dotenv==1.0.0
pyyaml==6.0.1

# Console interface and formatting
rich==13.7.0
colorama==0.4.6

# System monitoring and performance
psutil==5.9.8

# Data handling
json5==0.9.14

# Type hints and compatibility
typing-extensions>=4.8.0

# SSL and security
certifi>=2023.11.17

# Development and testing (optional)
pytest==7.4.4
pytest-asyncio==0.21.1

# Performance optimization for ARM
uvloop>=0.19.0; sys_platform != "win32"

# Additional utilities
setuptools>=68.0.0
wheel>=0.41.0

# FIXED VERSIONS - Tested combinations that work together:
# - aiohttp 3.9.5 + Python 3.9+ on ARM64
# - aiohttp 3.8.6 as fallback for compatibility issues
# - All other packages at stable versions
#
# If you encounter TCP_NODELAY or other aiohttp issues:
# 1. pip uninstall aiohttp
# 2. pip install aiohttp==3.8.6
# 3. Or try: pip install aiohttp==3.9.1
#
# For installation on Raspberry Pi:
# pip install --only-binary=all -r requirements.txt
